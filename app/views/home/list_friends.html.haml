= render :partial => 'user_header', :locals => {:what => "friends"}

= render :partial => 'users_list', :locals => {:users => @friends}


- content_for :side do
  = render :partial => 'side_contents', :locals => {:user => @user}
