
%h3= pluralize(@user_list.count, "search result")

= render :partial => 'users_list', :locals => {:users => @user_list}

- if current_user
  - content_for :side do
    = render :partial => 'side_contents', :locals => {:user => current_user}